Step53 — Alternative A: очищення route map у runtime

Що зроблено:
1) Розширено API адаптера (`src/ui/sws_v2/sws_adapter.js`):
   - `clearRoute(screenId)`
   - `clearAllRoutes()`

2) Розширено Debug Center (`src/ui/ui_debug_center.js`):
   - у блоці `Alternative A adapter controls` додано кнопки:
     - `Clear route`
     - `Clear all routes`
   - результат дій показується у JSON output разом із health/routes snapshot.

3) Додано unit test (`test/sws_adapter.test.js`):
   - перевірка очищення одного route та повного очищення route map.

Навіщо:
- Для ручної валідації міграції потрібен безпечний спосіб відкотити runtime routes без перезавантаження коду.
