
Ок, пройшовся по твоєму чек-плану і звірив з тим, що ми реально зробили в гілці/архівах під час цих етапів.
Нижче — поетапно: статус + що саме є/нема, і в кінці загальний %.
________________________________________
Етап 0 — Runtime Entries + Boot OK
Статус: ✅ виконано
Що є:
•	Boot OK marker показується (ти сам дав лог з __SDO_BOOT_OK__).
•	Runtime entries (список dist ресурсів) показуються (Count: 39).
Оцінка: 100%
________________________________________
Етап 1 — Debug Center UI + кнопка
Статус: ✅ виконано
Що є:
•	Кнопка 🧪 поруч із налаштуваннями.
•	Debug Center відкривається як SWS модалка.
Оцінка: 100%
________________________________________
Етап 2 — Module Health Report (включно зі storage)
Статус: ✅ виконано
Що є:
•	Health репорт з ✅/❌.
•	Storage канал виправили: sdo.api.storage + smoke-test get/set/del.
Оцінка: 100%
________________________________________
Етап 3 — Backup v2 export core + dry-run test
Статус: ✅ частково виконано
Що є:
•	backup_v2_export_core.js існує.
•	Debug Center має export dry-run.
Чого нема за планом:
•	“UI викликає export тільки через ядро” — не гарантовано, бо реальний export у UI ще живе в ui_core.js і частково дублює логіку.
Оцінка: ~75%
________________________________________
Етап 4 — Backup v2 import core (dry-run) + тест
Статус: ✅ частково виконано
Що є:
•	backup_v2_import_core.js існує.
•	Dry-run працює.
•	Debug Center показує report.
Чого нема за планом:
•	“UI кнопка Import показує помилки через IO report” — у нас є debug-репорти, але уніфікованого IO-report в усіх імпорт/експорт потоках зараз немає як стандарт.
Оцінка: ~80%
________________________________________
Етап 5 — Apply імпорт напряму (templates → settings → navigation → tableStore) без legacy importBackup
Статус: ⚠️ виконано частково, але іншим шляхом
Що є:
•	Реальний імпорт ZIP v2 тепер гарантовано застосовує datasets (ми додали прямий tableStore.importTableData після importBackup), і дані вже відновлюються.
•	Є inspect ZIP journals і zip self-test.
Чого нема / відхилення від плану:
•	“без legacy importBackup” — НЕ виконано. Ми досі використовуємо sdoInst.importBackup(...) для структури/шаблонів і лише “додатково” застосовуємо datasets.
•	“replace: dry-run → confirm → wipe → apply” — confirm/wipe є частково (деякі сценарії), але не як єдиний формалізований пайплайн.
Оцінка: ~60%
________________________________________
Етап 6 — Контроль розбіжностей + confirm діалоги (template missing, columns mismatch)
Статус: ❌ не реалізовано як система
•	Немає повної логіки confirm для mismatch/відсутніх шаблонів у ZIP v2 імпорті.
•	Нема “Simulate mismatch” тесту.
Оцінка: 0–10% (по факту — 0, бо немає завершеної предметної логіки)
________________________________________
Етап 7 — Вимкнути legacy backup v1
Статус: ❌ не реалізовано
•	Старі zip (backup.json) ще працюють / не вимкнені.
Оцінка: 0%
________________________________________
Build / src-dist
Етап 8 — Відновити build-скрипт (scripts/build.mjs)
Статус: ❌
Ми це не робили. У repo package.json посилається на scripts/build.mjs, але повноцінно не відновлено.
Оцінка: 0%
Етап 9 — build_meta.json + показ у Debug Center
Статус: ❌
Оцінка: 0%
Етап 10 — esbuild bundling src → dist
Статус: ❌
Оцінка: 0%
Етап 11 — dist policy (gitignore/verify-dist)
Статус: ❌
Оцінка: 0%
Етап 12 — прибрати дублювання src/dist у дев-циклі
Статус: ❌
Ми досі синхронізуємо вручну “і в src, і в dist” — це протилежність цього етапу.
Оцінка: 0%
________________________________________
Додатково (поза планом, але реально зроблено)
•	✅ Нумерація по дереву (core numbering_core.js)
•	✅ Відображення номерів у швидкій навігації
•	✅ meta.path для ZIP v2 на основі tree numbering
•	✅ Seed “welcome” + one-time cleanup при першій дії користувача (space/journal)
•	✅ Debug inspect ZIP journals (rows/cols/template/path)
•	✅ ZIP store self-test модуль
Це суттєва предметна робота, але вона не була частиною build-плану.
________________________________________
Загальний відсоток реалізації плану
Якщо рахувати строго тільки твій список Етап 0–12 (13 етапів):
•	Етап 0: 100%
•	Етап 1: 100%
•	Етап 2: 100%
•	Етап 3: 75%
•	Етап 4: 80%
•	Етап 5: 60%
•	Етап 6: 0%
•	Етап 7: 0%
•	Етап 8–12: 0%
Середнє по 13 етапах ≈
(100+100+100+75+80+60) / 13 = 515 / 13 = 39.6% ≈ 40%
📌 Відповідь: реалізовано приблизно 40% плану (по етапах 0–12), але при цьому додатково зроблено кілька важливих предметних речей поза цим планом (нумерація/quicknav/path/seed/debug tools).
________________________________________
Якщо хочеш — наступним кроком я:
1.	оновлю цей план, додавши “реально зроблені” етапи (нумерація/seed/path/debug inspect) як окремі,
2.	і переформатую “backup v2 apply” так, щоб він відповідав пунктам 5–7 (без legacy importBackup, з mismatch-confirm).

